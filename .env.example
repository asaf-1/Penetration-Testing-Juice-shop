# Default target when running outside Docker.
TARGET_URL=http://localhost:3000

# Docker Compose uses an isolated internal network and targets this service URL.
DOCKER_TARGET_URL=http://juice-shop:3000

# Optional: replace latest with a pinned tag or digest for a fully reproducible interview demo.
JUICE_SHOP_IMAGE=bkimminich/juice-shop:latest

